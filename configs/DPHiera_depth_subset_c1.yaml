dataset_ratio: 0.4

model:
  name: HieraDPT
  out_chans: 1
  pretrained_model: "exp/Hiera2_MAE/checkpoints/last.ckpt"

  hiera:
    use_bn: True
    decoder_embed_dim: 512

dataset:
  output_type: "depth"

optimizer:
  name: AdamW
  lr: 0.0005

scheduler:
  name: linear_cosine
  warmup: 2000
  cycle_k: 1